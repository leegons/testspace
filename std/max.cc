#include <cstdio>
#include <algorithm>

int main() {
    int a = 100;
    int b = 32;
    printf("max(%d, %d) = %d\n", a, b, std::max(a, b));
    return 0;
}


/* vim: set expandtab ts=4 sw=4 sts=4 tw=100: */
